/*class raindrop {
    constructor(x, y) {
      this.body = Bodies.rectangle(x, y);
      this.width = width;
      this.height = height;
      
      this.position.x = random(0,400);
      this.position.y = random(0,400);
      this.fill("green")


      World.add(world, this.body);
    }

    display(){
      
    }
  }*/